# <%= @fight.name %>
### Sequence <%= @fight.sequence %>
<% if active_effects(@fight).any? -%>
```diff
<%= active_effects(@fight).map { |effect| fight_effect(effect) }.join("\n ") %>
```
<% end -%>
<% @fight.shot_order.each do |shot, characters| -%>
## Shot <%= shot %>
<% characters.each do |character| -%>
- **<%= character.name %>**
<% if character.action_values["Type"] == "PC" || character.action_values["Type"] == "Ally" -%>
<% if wounds_and_impairments(character).present? -%>
 <%= wounds_and_impairments(character) %>
<% end -%>
<%-
  main_attack = character.action_values["MainAttack"]
  secondary_attack = character.action_values["SecondaryAttack"]
  fortune_type = character.action_values["FortuneType"]
-%>
 <%=
 [
   action_value(character, main_attack),
   action_value(character, secondary_attack),
   action_value(character, "Defense"),
   fortune_value(character),
   action_value(character, "Toughness"),
   action_value(character, "Speed"),
 ].compact.join(" ")
%>
<% if character.character_effects.where(fight: @fight).any? -%>
  ```diff
 <%= character.character_effects.where(fight: @fight).map { |effect| character_effect(effect) }.join("\n ") %>
 ```
<% end -%>
<% end -%>
<% end -%>
<% end -%>
